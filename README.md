Three-card flow application made with <3 for Eave :)

## Getting Started

1. `npm install`
2. `npm run dev`

## Scripts

- `npm t`: run tests (app needs to be running at localhost:3000)
- `npm run dev`: run app in development mode
- `npm run build && npm run start`: build and run in production mode
